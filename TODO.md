ondb use names from excel export
convert names from excel uploads to proper db names

on data context for delete, update and create, it calls a state but that state needs to be passed in and causes issues
